<?php

/*
 * Copyright (c) 2026 Besnovatyj. Licensed under the MIT License.
 */

declare(strict_types=1);

/**
 * Дефолты ядра поиска на Manticore.
 *
 * Значения `params.*` переопределяются через модуль настроек `yii2-cms-config` (опции объявлены
 * в config/options.php). Дефолты подобраны так, чтобы на обычной установке — демон на той же
 * машине, без авторизации — ядро заработало без единой настройки.
 */
return [
    'id'     => 'SearchManticore',
    'params' => [
        'iconClass' => 'bi bi-lightning-charge',

        /** Адрес демона. В docker-сборке — имя сервиса, на обычном сервере — 127.0.0.1. */
        'host' => '127.0.0.1',

        /** Порт SQL-интерфейса демона (протокол MySQL). */
        'port' => 9306,

        /**
         * Учётная запись демона. Пусто — подключаться анонимно: у Manticore авторизация
         * выключена по умолчанию, и на закрытом от сети порту это рабочий вариант.
         */
        'username' => '',

        /**
         * Имя секрета с паролем. Сам пароль в настройках не хранится: он читается из
         * `/run/secrets/<имя>`, а при отсутствии файла — из одноимённой переменной окружения.
         */
        'passwordSecret' => 'MANTICORE_PASSWORD',

        /**
         * Имя таблицы индекса. Пусто — вычисляется само из имени базы проекта, чтобы несколько
         * сайтов могли пользоваться одним демоном, не мешая друг другу.
         */
        'table' => '',

        /**
         * Морфология индекса. `auto` — по языку приложения: для русского лемматизатор со всеми
         * разборами омонимов, для остальных языков английский стеммер.
         *
         * Смена значения требует полной переиндексации: разобранные формы слов лежат в индексе,
         * а не вычисляются при запросе. Ядро пересоздаёт таблицу само в начале пересборки.
         */
        'morphology' => 'auto',

        /**
         * Подсказки «возможно, вы имели в виду».
         *
         * Требуют словаря подстрок, из-за которого индекс вырастает в несколько раз. На сайте в
         * тысячу документов это мегабайты; на большом корпусе имеет смысл выключить.
         */
        'suggestions' => true,

        /**
         * Допуск опечаток: сколько букв в слове может не совпасть. 0 — не искать с опечатками
         * даже когда они разрешены в настройках фасада.
         */
        'fuzzyDistance' => 2,

        /**
         * Раскладки клавиатуры для распознавания запроса, набранного не в той раскладке
         * («ghbdtn» → «привет»). `auto` — по языку приложения, пусто — не распознавать.
         */
        'layouts' => 'auto',

        /**
         * Сколько совпадений демон держит в памяти на один запрос. От этого зависят глубина
         * листания и точность счётчиков — и общего числа найденного, и цифр на вкладках.
         */
        'maxMatches' => 5000,

        'directories' => false,
    ],
];
